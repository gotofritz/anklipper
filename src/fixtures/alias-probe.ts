// Import target for the path-alias harness test. Lives under `src/` because
// that is what `@/` resolves to; a fixture outside srcDir could not prove it.
export const ALIAS_PROBE = "resolved via @/";
