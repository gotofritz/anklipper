# Prose wraps at 80; tables, code, and links cannot, so exempt them.
default: true

MD013:
  line_length: 80
  tables: false
  code_blocks: false
  headings: false

# Table pipe padding. Fights hand-maintained tables for no readability gain.
MD060: false
